name: Bot Shopee Telegram
on: workflow_dispatch
jobs:
  enviar:
    runs-on: ubuntu-latest
    steps:
      - name: Manda oferta pro Telegram
        run: |
          curl -X POST "https://api.telegram.org/bot${{ secrets.TOKEN }}/sendMessage" \
          -d chat_id=@robinairdraktutoriais \
          -d text="OFERTA RELÂMPAGO - CHURRASQUEIRA PORTÁTIL É DESMONTÁVEL COM GRELHA
          
          LINK DO PRODUTO : https://s.shopee.com.br/6ffHxillyX"